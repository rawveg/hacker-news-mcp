# Hacker News MCP Server Environment Variables

# API Key (if needed in the future)
HN_API_KEY=

# Logging level (debug, info, warning, error, critical)
LOG_LEVEL=info

# FastMCP configuration
# Uncomment to enable JSON parsing for tool arguments (for older LLMs)
# FASTMCP_TOOL_ATTEMPT_PARSE_JSON_ARGS=1

# Server configuration
# HOST=0.0.0.0
# PORT=8000
